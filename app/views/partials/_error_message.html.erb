<div class='error-message'>
    <h2>
        <%= "#{pluralize(errors.count(), 'error')} #{consequence}" %>
    </h2>
    <ul>
        <% errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
    </ul>
</div>